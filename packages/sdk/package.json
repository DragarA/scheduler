{
  "name": "@scheduler/sdk",
  "version": "0.1.0",
  "private": false,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "generate": "openapi --input ../../apps/api/openapi.json --output src/generated --client fetch --name SchedulerApiClient",
    "build": "tsc -p tsconfig.build.json",
    "clean": "rm -rf dist src/generated"
  },
  "devDependencies": {
    "openapi-typescript-codegen": "^0.29.0",
    "typescript": "^5.6.0"
  }
}
